# Point-Cloud-Annotator
A web application that loads and displays a LAZ point cloud using Potree which provides an interface for a user to select any point on the point cloud and attach an annotation.
